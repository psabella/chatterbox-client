var Messages = {

  // print: () => {
  //   App.startSpinner();
  //   var data = App.fetch(App.stopSpinner);
  //   for (var i = 0; i < data.results.length; i++) {
  //     var message = data.results[i];
  //     MessagesView.renderMessage(message);
  //   }
  // }

};